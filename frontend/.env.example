# Frontend Configuration
# When deployed together with backend on the same domain, leave empty or use /api
# When frontend is deployed separately on Vercel/Netlify, set to backend domain:
# VITE_API_URL=https://your-backend.onrender.com/api
